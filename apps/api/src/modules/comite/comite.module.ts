import { Module } from '@nestjs/common';

// Revisión del comité: consulta de solicitudes pendientes, aprobación y rechazo.
// Capas: domain / application / infrastructure / presentation (CLAUDE.md §2.2).
@Module({})
export class ComiteModule {}
